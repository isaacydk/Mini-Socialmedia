package Controller;

import Backend.Post;
import Backend.Database;

public class CreatePost {

    private Post post;
    private Database db;
     
    public CreatePost(Post p, Database db){
        // Database db = new Database();
        // db.insertPost(p.getUserId(), p.getContent(), p.getTimestamp());{
        
    }
}
